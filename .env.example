# Copy to .env.local and restart pnpm dev after changing values.
# Example backend URL; replace with your backend's actual API prefix.
VITE_API_BASE_URL=http://localhost:8000/api
VITE_USE_MOCK_API=true
